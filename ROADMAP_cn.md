# Anteumbra 路线图

[English](ROADMAP.md)

> **最新发布版**：v1.0.28（2026-07-18 已打 Tag 并发布）
> **产品愿景**：面向单机与小型 Web 业务的安全运营工具，覆盖被动文件检测、访问日志行为分析、攻击者画像、人工响应和标准 SIEM 输出。
> **源码状态**：`main` 包含 1.0.28 之后尚未发布的架构收口。当前源码已通过 486 项非浏览器测试、41 项浏览器测试、Ruff 和 115 模块导入扫描；下一次 bug 修复版发布前仍须重新完成 Wheel、源码安装、Docker、Tag 与 PyPI 验证。

---

## 项目现状

Anteumbra 已经完成最初的 Trident 改名和打包修复。当前 1.0.x 的目标是保持 PyPI、
源码与 Docker 为同一个产品、同一条配置和运行链路。

### 1.0.x 已完成能力

| 领域 | 当前状态 |
|------|----------|
| 打包 | `pip install anteumbra` 包含配置模板、页面、翻译、静态资源、插件与 YARA 规则。 |
| 运行实例 | `anteumbra install <dir>` 创建 `config.toml`、`.env`、规则、日志、数据目录和初始管理员密码。 |
| CLI 配置 | `anteumbra config init/set/env/wizard/access-log/validate/reload` 覆盖首次配置与常见修改。 |
| Web 流程 | 仪表盘、监控、扫描、隔离、还原、标记误报、设置、日志、审计和健康检查已经串联。 |
| 访问日志 | 支持 Nginx、Apache、Tomcat 和自定义格式，并按站点分析。 |
| 日志流 | SSE 不再输出连接噪音，可加载历史监控日志并展示通知投递状态。 |
| Docker | 容器运行完整 `anteumbra run`，创建 Docker 默认配置、输出首次密码并接受健康检查。 |
| 运行可靠性 | 多站点资源具有明确所有权；毒 JSONL 消息进入死信；有界队列提供背压；基线扫描和降级能力可见。 |
| YARA 治理 | 27 个内置规则文件独立编译；错误文件隔离；失败重载保留有效规则；THOR 分片可独立验证。 |
| 架构守卫 | 强制分层/导入边界、源码测试导入与 Wheel/源码一致性，已解决债务不会重新混入构建。 |
| 运行时所有权 | 单个 `RuntimeLifecycle` 管理启动、状态、反向关闭与每 App 登录限流，不再使用 Web/Launcher 模块可变状态。 |
| 接入契约 | `RuntimeContainer` 通过 Domain Protocol 暴露必需服务；Web 与插件不再读取 Metrics、聚类、ThreatGraph 或 Notifier 私有状态。 |
| 文档 | README、用户手册、架构、路线图、更新日志、发布指南和内存马工具说明均提供中英文导航。 |

### 已确认边界

- Anteumbra 可作为单机安全运营工具使用，但不能宣传为 WAF、EDR、SIEM、集中式主机管理或分布式高可用平台的替代品。
- 当前架构是模块化单体：`launcher.py` 是唯一组合根，可替换服务实现 `domain/runtime.py` 与 `domain/service_ports.py` 中的契约。
- Registry、指标、通知、隔离、扫描历史、仪表盘汇总、ThreatGraph 画像/信誉和 SQLite 影子键都已携带站点身份；无法归属的历史数据明确保留在 `legacy` 桶。
- 登录失败限流由每个 App Runtime 的 `LoginRateLimiter` 所有。内存后端符合当前单进程定位；多 Worker 或分布式部署仍需共享后端。
- Docker 中 `yara-python` 为必装能力；`py-tlsh` 与 `ssdeep` 受 Python/基础镜像兼容性影响，可按设计降级。

---

## v1.0.20 至 1.0.28 之后的清理线

| 版本 | 主题 | 状态 |
|------|------|------|
| 1.0.20 | 重构基线与架构守卫 | 完成 |
| 1.0.21 | 首次运行与启动可靠性 | 完成 |
| 1.0.22 | CLI 配置流程与绑定地址 | 完成 |
| 1.0.23 | Web 审计入口、安静 SSE、通知可见性 | 完成 |
| 1.0.24 | Tomcat 日志分析与 CLI 预设 | 完成 |
| 1.0.25 | Docker 完整运行链路与文档同步 | 完成 |
| 1.0.26 | 多站点生命周期、毒消息、事务隔离、真实 UI E2E | 已发布 PyPI |
| 1.0.27 | 可观测性、背压、可信代理、SIEM、YARA 治理、还原去重 | 已发布 PyPI |
| 1.0.28 | 站点隔离服务、JSON 主存储/SQLite 影子、架构守卫 | 已发布 PyPI |
| `main`（未发布） | Launcher 与登录限流实例所有权、ThreatGraph 站点隔离、服务 Protocol、公开模块 API、Waitress 确定性关闭、双语文档与严格 CI | 验证中 |

---

## 发布就绪检查

| 优先级 | 检查项 | 状态 |
|--------|--------|------|
| P0 | 更新未发布源码对应的中英文更新日志与文档 | `main` 已完成；最终版本号待定 |
| P0 | 在全新虚拟环境验证发布 Wheel | 下一 Tag 前待执行 |
| P0 | 在全新虚拟环境验证可编辑源码安装 | 下一 Tag 前待执行 |
| P0 | 验证 Docker build/run/health/检测链路 | 下一 Tag 前待执行 |
| P0 | 核对 README 命令与真实 CLI 输出 | 最终安装冒烟时执行 |
| P1 | 部署、架构与 Web 回归测试 | 源码已通过 486 项非浏览器测试、41 项 UI 测试 |
| P1 | 仅在以上检查全部通过且工作区干净后打 Tag | 待执行 |
| P1 | 验证 Trusted Publishing 与已发布 PyPI 包安装 | 待执行 |

---

## v1.1.0 前的架构收口

目标是让一个 AI 或工程师可以独立实现模块，通过明确契约接入，而不依赖隐藏全局状态。

| 优先级 | 工作项 | 状态 | 原因 |
|--------|--------|------|------|
| P0 | 引入 Application/Runtime 上下文 | 完成 | 用显式依赖替代分散查找。 |
| P0 | 删除全局配置注册表 | 完成 | 每个 Runtime 拥有一个 `TomlConfigProvider`。 |
| P0 | 为 Scanner、聚类、ThreatGraph、Notifier、SIEM、SSE、WAL、WAF 和插件定义稳定契约 | `main` 完成 | 模块通过聚焦 Protocol 接入。 |
| P0 | Launcher 资源归实例所有并删除模块生命周期门面 | `main` 完成 | App 与测试 Runtime 不再共享状态。 |
| P0 | Threat intelligence 与公开查询完成站点隔离 | `main` 完成 | 同 IP、路径和画像不会跨站串联。 |
| P1 | 删除跨模块私有状态访问 | `main` 完成 | Interfaces 使用公开快照和查询 API。 |
| P1 | 增加导入、公开 Port、文档配对和严格 CI 守卫 | `main` 完成 | 防止架构和工程治理倒退。 |
| P1 | 将登录失败限流移入 App 所有服务 | `main` 完成 | App 与测试 Runtime 不再共享 Interface 模块可变状态。 |
| P1 | 仅在触碰旧模块时修复历史乱码/旧式注释 | 持续 | 避免无风险收益的全仓机械重写。 |

---

## v1.1.0 - 多站点运营与扩展 SDK

只有架构收口版通过 Wheel、源码、Docker 与 PyPI 全部检查后才开始功能开发。

| 优先级 | 工作项 |
|--------|--------|
| P0 | 增加站点选择器与完整站点范围的仪表盘/导航状态，不改变底层隔离模型。 |
| P0 | 发布基于现有 Protocol 的插件、日志分析器、Scanner 与 Adapter 开发模板。 |
| P1 | 增加第三方模块契约测试，并在激活前验证插件元数据和配置。 |
| P1 | 增加面向运营人员的多站点迁移与备份流程。 |
| P2 | 在保留当前进程内事件总线的前提下引入强类型事件负载。 |

---

## v1.2.0 - 生产加固

| 优先级 | 工作项 |
|--------|--------|
| P0 | CI 中增加全新 PyPI/Wheel 运行实例冒烟测试。 |
| P0 | Docker 多架构镜像发布（`linux/amd64`、`linux/arm64`）。 |
| P1 | 可选 Redis/缓存 Session 后端。 |
| P1 | Prometheus 兼容指标端点或明确的抓取文档。 |
| P1 | Nginx/Caddy 反向代理部署示例。 |
| P2 | SIEM 实时 syslog 流，而不只是文件导出。 |
| P2 | `data/`、`logs/`、`config.toml`、`.env` 的备份/还原文档。 |

---

## v1.3.0 - 情报扩展

| 优先级 | 工作项 |
|--------|--------|
| P1 | 攻击者画像 Geo-IP 富化。 |
| P1 | AbuseIPDB/MISP 类威胁情报源。 |
| P1 | 管理员 2FA/TOTP 与 API Key 管理。 |
| P2 | 更完整的安全审计报告导出。 |

---

## v2.0.0 - 异步与分布式核心

这些属于未来架构，不是 1.0.x 清理项。

| 优先级 | 工作项 |
|--------|--------|
| P0 | 用异步事件总线替换当前队列/线程模型。 |
| P0 | 所有内部事件使用强类型 Schema。 |
| P1 | 基于 Redis Pub/Sub 或等价能力的分布式部署。 |
| P1 | 插件市场或可通过 pip 安装的外部插件。 |
| P2 | 在 REST 之外提供 GraphQL 或更丰富查询 API。 |

---

## Trident 历史

Anteumbra 继承了 Trident 的文件监控、YARA 检测、日志启发式、SIEM 导出、插件思路、
内存马工具和 Web 仪表盘。1.0.x 的工作是把这些能力整理成一个可安装、可测试、边界明确的产品。
